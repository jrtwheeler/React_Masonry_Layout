import React from "react";

const Card = ({images}) => {

    return (
        <>
            {images.map((img, index) => (
                <
            ))}
        </>
    );
};

export default Card;